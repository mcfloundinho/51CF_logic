#ifndef GAME_H
#define GAME_H

#include "definition.h"
#include <vector>
#include <string>
using std::vector; 
using std::string;
using std::size_t;

class Game
{
public:
	void loadMap(string filename,size_t index);
	void Run(vector<vector<>>)
};

#endif // !GAME_H

